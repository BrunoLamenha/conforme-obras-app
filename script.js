function navegar(destino) {
    if (destino === 'vistoria') {
        alert("Redirecionando para o fluxo de Vistoria...");
        // Próximo passo: carregar a lista de empreendimentos estruturais e arquitetônicos
    } else if (destino === 'cadastros') {
        alert("Redirecionando para o fluxo de Cadastros...");
        // Próximo passo: escolher da lista ou cadastrar novo empreendimento (Wizard)
    }
}
